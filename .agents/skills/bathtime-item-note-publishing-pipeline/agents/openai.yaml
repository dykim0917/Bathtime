interface:
  display_name: "Bathtime Item Note Publishing Pipeline"
  short_description: "아이템 각도 설계부터 비공개 DB draft와 preview 검증까지 실행"
  default_prompt: "Use $bathtime-item-note-publishing-pipeline to create the item angle, research it, build seed files, produce web content, implement ArchiveContent, apply it as a private draft, and verify preview."

policy:
  allow_implicit_invocation: true
