interface:
  display_name: "Bathtime Item Note Web Content Producer"
  short_description: "아이템 시드를 웹 게시용 원고와 이미지 배치 브리프로 정리"
  default_prompt: "Use $bathtime-item-note-web-content-producer to turn these item seed files into a publish-ready Bathtime Item Note web content package."

policy:
  allow_implicit_invocation: true
