interface:
  display_name: "Bathtime Archive Content Implementer"
  short_description: "웹 콘텐츠 패키지를 ArchiveContent, DB draft, preview 검증까지 반영"
  default_prompt: "Use $bathtime-archive-content-implementer to update spot-seed.archive-content.ts from this seed directory, generate archive DB upsert artifacts, apply as draft if requested, and verify the preview output."

policy:
  allow_implicit_invocation: true
