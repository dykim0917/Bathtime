# AGENTS.md

`im-not-ai/` is not generated cleanup output.

Keep this directory in the workspace. It provides the local `humanize-korean` Codex skill at:

```text
im-not-ai/codex/skills/humanize-korean/SKILL.md
```

The Codex skill uses `references` as a symlink to the companion reference files under:

```text
im-not-ai/.claude/skills/humanize-korean/references/
```

Do not move this directory to external cleanup archives.
